$('.render-list-comments').html('<%= j render "comments/comments", comments: @comments %>');
$('.action-<%= @parent.id %>').html(
  '<%= j render "comments/form", comment: @comment, post_id: @parent.post.id, parent_id: @parent.id %>')
