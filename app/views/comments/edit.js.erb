$('.render-list-comments').html('<%= j render "comments/comments", comments: @comments %>');
$('.action-<%= @comment.id %>').html(
  '<%= j render "comments/form", comment: @comment, post_id: @post.try(:id), parent_id: @parent.try(:id) %>')
